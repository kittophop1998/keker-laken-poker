# สรุปการแก้ไขเกม Kaker Poker

## ✅ การเปลี่ยนแปลงหลัก

### 1. **แจกไพ่คนละ 16 ใบ**
- ผู้เล่นแต่ละคนได้รับ **16 ใบ**
- ระบบจะสร้างไพ่เพียงพอตามจำนวนผู้เล่น:
  - 2 คน = 32 ใบ (16+16)
  - 3 คน = 48 ใบ (16+16+16)
  - 4 คน = 64 ใบ (16×4)
- ใช้ **8 สัตว์**: แมลงสาบ, หนู, แมลงเขียว, แมงมุม, แมลงวัน, ค้างคาว, กบ, แมงป่อง
- สัตว์แต่ละชนิดจะมีจำนวนเท่ากัน

### 2. **กลไกการเล่นที่ถูกต้อง**
#### การส่งไพ่:
- ผู้เล่นเลือกไพ่จากมือและส่งให้ผู้เล่นคนอื่น
- บอกว่าเป็นสัตว์อะไร (อาจจริงหรือโกหกก็ได้)
- ไพ่จะถูกถือโดยระบบจนกว่าจะมีการทายหรือส่งต่อ

#### การทาย:
- ผู้รับเลือกว่าคนส่ง **พูดจริง** หรือ **โกหก**
- เปิดไพ่ตรวจสอบ:
  - **ทายถูก** → คนส่งเก็บไพ่ และเป็นคนเล่นต่อ
  - **ทายผิด** → คนทายเก็บไพ่ และเป็นคนเล่นต่อ

#### การส่งต่อ (Pass):
- ผู้รับสามารถ**เปิดดูไพ่** (เฉพาะตัวเอง)
- จากนั้นเลือกผู้เล่นคนอื่นและ**ส่งไพ่ต่อ**
- บอกว่าเป็นสัตว์อะไร (อาจเหมือนหรือต่างจากคนก่อนก็ได้)

### 3. **เงื่อนไขการแพ้**
ผู้เล่นแพ้เมื่อ:
1. **ไพ่ในมือหมด** (cards.length === 0)
2. **มีสัตว์ชนิดเดียวกัน 4 ใบในไพ่ที่เก็บ** (deadCards)

เดิม: ตรวจสอบว่าไพ่ในมือและไพ่ที่เก็บหมดพร้อมกัน (ผิด)

### 4. **การแสดงผล UI**
- แสดงจำนวนไพ่ในมือแต่ละคน
- แสดงไพ่ที่เก็บแบบรวมกลุ่ม (เช่น แมลงสาบ x3)
- เตือนเมื่อมีสัตว์ 4 ตัวเหมือนกัน (⚠️ กำลังจะแพ้)
- แสดง log การเล่นแบบเรียลไทม์

### 5. **ผู้เล่นที่ลงไพ่ต่อ**
- คนที่**แพ้การทาย**จะเป็นคนเล่นต่อ (ลงไพ่ใหม่)
- ทำให้เกมมีจังหวะและไม่วนตามลำดับแบบเดิม

## 📁 ไฟล์ที่แก้ไข

### Backend: `pages/api/socketio.ts`
- สร้างสำรับไพ่ 16 ใบ (4 สัตว์ × 4 ใบ)
- เพิ่ม `currentCardSender` เพื่อติดตามคนส่งไพ่
- แก้ไข event handlers:
  - `sendCard` - ส่งไพ่จากมือให้ผู้เล่นอื่น
  - `sendRevealedCard` - ส่งไพ่ที่เปิดดูแล้วต่อให้คนอื่น
  - `challenge` - ทายว่าจริงหรือโกหก
  - `passCard` - เปิดดูไพ่และเตรียมส่งต่อ
- แก้ไข `checkGameEnd()` ให้ตรวจสอบเงื่อนไขการแพ้ถูกต้อง

### Frontend: `app/game/page.tsx`
- จัดเรียงลำดับสัตว์ใหม่ (สัตว์ในเกมอยู่ด้านหน้า)
- แก้ไข `sendRevealedCard` ไม่ต้องส่ง `cardId`
- UI แสดงเตือนเมื่อมีสัตว์ 4 ตัวเหมือนกัน
- ลบเงื่อนไขการเตือน "มีสัตว์ครบทุกชนิด" (เพราะมีแค่ 4 สัตว์)

### เอกสาร
- สร้าง `GAME_RULES.md` - กฎการเล่นแบบละเอียด
- สร้าง `CHANGES.md` - สรุปการเปลี่ยนแปลง (ไฟล์นี้)

## 🎮 วิธีทดสอบ

1. เริ่มเซิร์ฟเวอร์:
```bash
npm run dev
```

2. เปิด browser 2 หน้าต่าง:
   - หน้าแรก: สร้างห้อง
   - หน้าที่สอง: เข้าร่วมห้องด้วยรหัสเดียวกัน

3. ทดสอบ:
   - [x] แจกไพ่คนละ 16 ใบ
   - [x] ส่งไพ่ให้เพื่อน พร้อมบอกว่าเป็นสัตว์อะไร
   - [x] ทายว่าจริงหรือโกหก
   - [x] เปิดดูไพ่และส่งต่อ
   - [x] ตรวจสอบการแพ้เมื่อไพ่ในมือหมด
   - [x] ตรวจสอบการแพ้เมื่อมีสัตว์ 4 ตัวเหมือนกัน

## 🔧 ข้อมูลเทคนิค

### สำรับไพ่
```typescript
const ANIMALS = ['แมลงสาบ', 'หนู', 'แมลงเขียว', 'แมงมุม', 'แมลงวัน', 'ค้างคาว', 'กบ', 'แมงป่อง']; // 8 ชนิด
const CARDS_PER_PLAYER = 16; // แจกคนละ 16 ใบ

// ระบบจะสร้างไพ่ตามจำนวนผู้เล่นอัตโนมัติ
function createDeck(numPlayers: number): Card[]
```

### Interface สำคัญ
```typescript
interface Room {
  id: string;
  players: Player[];
  gameStarted: boolean;
  currentPlayer: string | null;
  currentCard: Card | null;         // ไพ่ที่กำลังส่ง
  currentClaim: string | null;      // อ้างว่าเป็นสัตว์อะไร
  currentCardSender: string | null; // คนที่ส่งไพ่
  deck: Card[];
}

interface Player {
  id: string;
  name: string;
  cards: Card[];      // ไพ่ในมือ
  deadCards: Card[];  // ไพ่ที่เก็บไว้
}
```

### Events
- `createRoom` - สร้างห้อง
- `joinRoom` - เข้าร่วมห้อง
- `startGame` - เริ่มเกม
- `sendCard` - ส่งไพ่จากมือ
- `sendRevealedCard` - ส่งไพ่ที่เปิดดูแล้ว
- `challenge` - ทายว่าจริงหรือโกหก
- `passCard` - เปิดดูไพ่
- `cardSent` - แจ้งว่ามีการส่งไพ่
- `revealCard` - แจ้งผลการเปิดไพ่
- `challengeResult` - แจ้งผลการทาย
- `gameOver` - จบเกม

## 🎯 สิ่งที่ได้แก้ไขตามคำขอ

✅ **แจกไพ่คนละ 16 ใบ** (ไม่ใช่รวมกัน 16 ใบ)  
✅ **กลไกการส่งไพ่และทาย** ตรงตามกฎเกม  
✅ **เงื่อนไขการแพ้** ถูกต้อง (ไพ่หมด หรือ มีสัตว์ 4 ตัวเหมือนกัน)  
✅ **คนที่แพ้การทายเป็นคนเล่นต่อ**  
✅ **UI แสดงผลชัดเจน** และเตือนเมื่อใกล้แพ้  
✅ **ระบบสร้างไพ่อัตโนมัติ** ตามจำนวนผู้เล่น  

---

**เกมพร้อมเล่นแล้ว! สนุกกับการโกหกและจับโกหกกันให้มันส์! 🃏🎉**
